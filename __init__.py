"""
# 今日运势 (Jrys)

为群友抽取今日运势并渲染成一张竖版海报，头像、背景与签文一次成图。

## 主要功能

- **今日运势海报**: 从内置文案库按权重抽签，把日期、运势评级、幸运星、签文与解签渲染到随机背景图上，并贴上圆形头像。
- **每日固定运势**: 默认同一用户当天抽到的运势固定，重复请求不会变来变去。
- **节假日高爆率**: 命中节假日列表的日期自动切换到节假日权重（默认大吉概率更高）。
- **背景原图回看**: `jrys_last` 可以重新取回上一次使用的无障碍原图。

## 使用方法

- **命令**: `/jrys`（别名 `/今日运势`、`/运势`）生成自己的运势海报，`/jrys [@某人]` 可以替群友抽一张。
- **AI 自动使用**: 对 AI 说"帮我看看今天运势"时，AI 会调用插件生成并发送海报。
- **关键词触发** (默认关闭): 打开后在群里单独发送 `jrys`、`今日运势`、`运势` 会直接出图且不唤醒 AI。

## 配置说明

- **图片与排版**: 宽度、高度、字体、头像尺寸与位置、各段文字 Y 轴坐标均可调，默认按 1080x1920 排版。
- **爆率权重**: 日常与节假日各有一组大吉 / 中吉 / 凶运权重，按分数段分配。
- **背景图池**: 背景图 URL 放在插件 `assets/backgroundFolder/*.txt` 中，每行一个，可自行增删；开启预缓存后会在加载时批量下载。
"""

import sys

# nekro 的热重载只丢弃顶层模块，子模块会留在 sys.modules 里继续跑旧代码；
# 清掉自己名下的子模块，/api/plugins/reload 才能真的加载到最新代码。
for _stale in [name for name in sys.modules if name.startswith(f"{__name__}.")]:
    sys.modules.pop(_stale, None)

from . import handlers
from .plugin import plugin

__all__ = ["plugin"]
